import { FormatoDescripcionAutosPipe } from './formato-descripcion-autos.pipe';

describe('FormatoDescripcionAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoDescripcionAutosPipe();
    expect(pipe).toBeTruthy();
  });
});

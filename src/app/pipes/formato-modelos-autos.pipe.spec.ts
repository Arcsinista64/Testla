import { FormatoModelosAutosPipe } from './formato-modelos-autos.pipe';

describe('FormatoModelosAutosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoModelosAutosPipe();
    expect(pipe).toBeTruthy();
  });
});
